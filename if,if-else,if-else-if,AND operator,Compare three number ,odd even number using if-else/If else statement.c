#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter the  Distinct two numbers=");
    scanf("%d%d",&a,&b);
    if (a>b)
    {
        printf("a is Greatest i.e %d",a);
    }
    else
    {
        printf("b is Greatest i.e %d",b);
    }
    }
