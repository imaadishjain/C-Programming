#include<stdio.h>
#include<math.h>

int sum(int a,int b)
{
    return (a+b);
}
int square(int x)
{
    return (x*x);
}
float circle_area(float r)
{
    return (3.14*r*r);
}
float triangle_area(float b,float h)
{
    return (0.5*b*h);
}

